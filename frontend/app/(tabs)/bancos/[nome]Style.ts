import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        padding: 16,
        backgroundColor: "#fff",
    },
    backButton: {
        flexDirection: "row",
        alignItems: "center",
        padding: 5
    },
    buttonVoltar: {
        fontSize: 18,
    },
    buttonContainer: {
        marginBottom: '4%'
    },
    icon: {
        marginTop: '1%',
        marginRight: 3
    }
});
